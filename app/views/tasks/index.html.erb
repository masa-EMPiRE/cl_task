<h1>タスク一覧</h1>

<ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.title, task %>|
          <%= link_to "削除", tasks_path(@task), method: :delete, data: { confirm: '削除してもよろしいですか？'} %>
      </li>
    <% end %>
</ul>

<hr>
<h2>タスク登録</h2>
<%= render "form" %>